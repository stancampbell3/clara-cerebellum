[company]
name = "Clara"
agent_name = "Clara"
greeting = "Hi! I'm Clara, a general-purpose assistant that learns as we talk. Ask me anything — if I don't already know, I'll go research it."

[server]
port = 8088
interface = "0.0.0.0"

[paths]
# For local dev, run lildaemon on the host (or point at whichever
# instance's port is reachable) and override with FRONTDESK_CONFIG.
fiery_pit_url = "http://localhost:6666"
static_path   = "/mnt/vastness/home/stanc/Development/clara-cerebrum/clara-frontdesk-poc/static"

service_username = "frontdesk-service"
service_password = "frontdesk-service-pw"

[log]
level = "debug"
format = "[${timestamp}] ${target} - ${message}"
